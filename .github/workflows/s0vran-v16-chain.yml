name: S0VRAN V16 Chain of Custody Render

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened]
    paths:
      - 'tools/s0vran-v16/**'

permissions:
  contents: read

jobs:
  render:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - name: Check out render branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}

      - name: Install render dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y ffmpeg fonts-dejavu-core fonts-liberation curl
          python3 -m pip install --upgrade pip
          python3 -m pip install requests numpy scipy librosa soundfile pillow "yt-dlp[default,curl-cffi]"

      - name: Render S0VRAN V16
        shell: bash
        run: |
          mkdir -p out
          set -o pipefail
          python3 tools/s0vran-v16/render.py 2>&1 | tee out/runner_v16.log

      - name: Upload render log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: S0VRAN_V16_RENDER_LOG
          retention-days: 3
          if-no-files-found: warn
          path: |
            out/runner_v16.log
            out/render_v16.log

      - name: Upload finished film
        uses: actions/upload-artifact@v4
        with:
          name: S0VRAN_V16_CHAIN_OF_CUSTODY
          retention-days: 3
          if-no-files-found: error
          path: |
            out/S0VRAN_CHAIN_OF_CUSTODY_V16_WEB.mp4
            out/S0VRAN_CHAIN_OF_CUSTODY_V16_MASTER.mp4
            out/S0VRAN_V16_SOUNDTRACK_EDIT.mp3
            out/S0VRAN_V16_POSTER.jpg
            out/S0VRAN_V16_CONTACT.jpg
            out/S0VRAN_V16_CLIMAX_CONTACT.jpg
            out/S0VRAN_V16_STORY_SCRIPT.md
            out/CREDITS.txt
            out/SOURCE_MANIFEST.json
            out/EDIT_QA.json
            out/runner_v16.log
            out/render_v16.log
